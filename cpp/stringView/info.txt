что такое stringview

class string_view{
    const char* data;
    size_t size;
}

он не владеет данными 
только смотрит на чужой буфер

значит копия быстро копирование только указателя и размера 
только чтеник 
не обязан быть нуль терминированным