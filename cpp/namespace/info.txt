namespace mylib {
    void sort(int* first, int* last);   // объявление (в заголовке)
}

// .cpp — опечатка: затесался const
namespace mylib {
    void sort(const int* first, int* last) { /*...*/ }  // ← новая функция!
}

void std::sort(){...}

чем второй вариант лучше? он проверит что у нас есть определение где-то

в первом варианет мы можем и объявить и определить функцию 
