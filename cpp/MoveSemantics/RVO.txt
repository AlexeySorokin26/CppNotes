Return value optimisation

X foo(){
    return X(); // RVO 
}

x bar(){
    X x;
    ...
    return x; // NRVO (named)
}

X qux(){
    X v1;
    X v2;

    bool cond;

    return cond ? v1 : v2; а тут ничего не поможет и будет копирование 
}

суть в том что мы подменяем место в котором создаем объект

в случае RVO делаем прям там где нам нужно в коде
X x = foo(); прям тут и создаем

в случае NRVO мы делаем что-то вроде ссылки в функции bar 

RVO и NRVO приоритетеней чем move семантика